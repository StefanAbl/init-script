apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - name: prometheus-external-config
    namespace: monitoring
    files:
      - Corefile
      - hosts
