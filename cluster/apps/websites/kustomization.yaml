apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: websites
resources:
  - image-automation.yml
  - blog
  - tphc
  - studi-conf
  # blog, tphc and studi-conf all serve from the external gateway now.
  - ../../policies/allow-from-external-ingress.yml
