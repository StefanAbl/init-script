apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-stack
  namespace: monitoring
spec:
  resources:
    requests:
      storage: 10Gi
  volumeMode: longhorn
  accessModes:
    - ReadWriteOnce
