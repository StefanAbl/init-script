[mysqld]
join_buffer_size = 16M

# tmp_table_size and max_heap_table_size should be equal
tmp_table_size = 64M
max_heap_table_size =64M

performance_schema = ON

innodb_buffer_pool_size = 1024M

innodb_log_file_size = 256M

max_connections = 70
